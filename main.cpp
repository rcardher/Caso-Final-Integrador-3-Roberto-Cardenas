#include "labmain.h"
#include <iostream>

int main() {
    std::cout << "Bienvenido al cargador de scripts" << std::endl;

    // Llamar a la función que solicita el nombre del archivo
    load_script();

    return 0;
}
